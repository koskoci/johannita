<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<% if @post.images.attached? %>
  <p>
    <strong>Images:</strong>
    <% @post.images_attachments.each do |image| %>
      <br>
      <%= image_tag image %>
      <%= link_to 'Remove', attachment_url(image.id), method: :delete %>
    <% end %>
  </p>
<% end %>

<%= link_to 'Back', posts_path %>
